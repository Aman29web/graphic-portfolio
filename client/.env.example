# Only needed when the API is NOT on the same origin.
# In development the Vite proxy forwards /api and /uploads to localhost:5000,
# so you can leave this file out entirely.
# VITE_API_URL=https://your-api-domain.com/api
